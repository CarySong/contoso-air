apiVersion: v1
kind: Service
metadata:
  name: contoso-air12
  namespace: dev
  labels:
    app.kubernetes.io/name: contoso-air12
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: contoso-air12
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000