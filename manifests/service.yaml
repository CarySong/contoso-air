apiVersion: v1
kind: Service
metadata:
  name: contoso-air1
  namespace: qa
  labels:
    app.kubernetes.io/name: contoso-air1
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: contoso-air1
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000