apiVersion: v1
kind: Service
metadata:
  name: contoso-air4
  namespace: dev
  labels:
    app.kubernetes.io/name: contoso-air4
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: contoso-air4
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000