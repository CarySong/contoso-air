apiVersion: v1
kind: Service
metadata:
  name: contoso-air5
  namespace: dev
  labels:
    app.kubernetes.io/name: contoso-air5
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: contoso-air5
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081